# PostgreSQL connection string
DATABASE_URL="postgresql://user:password@localhost:5432/animedb?schema=public"

# Public site URL, used for canonical links, sitemap and Open Graph
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Optional: only needed if/when auth is implemented (Phase 6)
# AUTH_SECRET=""
# AUTH_URL="http://localhost:3000"

# Optional secondary providers (Phase 7) - AniList itself needs no key
# JIKAN_BASE_URL="https://api.jikan.moe/v4"
# KITSU_BASE_URL="https://kitsu.io/api/edge"
# SIMKL_CLIENT_ID=""
